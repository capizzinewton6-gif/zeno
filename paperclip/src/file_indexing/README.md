# file_indexing

File indexing for fast search

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.file_indexing import FileIndexing

module = FileIndexing()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  file_indexing: true  # or false to disable
```

## Testing

```bash
pytest tests/file_indexing/
```
