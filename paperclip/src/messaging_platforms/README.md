# messaging_platforms

Multi-platform messaging integration

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.messaging_platforms import MessagingPlatforms

module = MessagingPlatforms()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  messaging_platforms: true  # or false to disable
```

## Testing

```bash
pytest tests/messaging_platforms/
```
