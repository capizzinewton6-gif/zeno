# keyboard_control

Keyboard input and hotkey automation

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.keyboard_control import KeyboardControl

module = KeyboardControl()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  keyboard_control: true  # or false to disable
```

## Testing

```bash
pytest tests/keyboard_control/
```
