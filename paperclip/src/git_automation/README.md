# git_automation

Git operations (clone, commit, push, etc.)

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.git_automation import GitAutomation

module = GitAutomation()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  git_automation: true  # or false to disable
```

## Testing

```bash
pytest tests/git_automation/
```
