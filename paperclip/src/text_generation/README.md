# text_generation

Text generation and formatting

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.text_generation import TextGeneration

module = TextGeneration()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  text_generation: true  # or false to disable
```

## Testing

```bash
pytest tests/text_generation/
```
