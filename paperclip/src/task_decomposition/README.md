# task_decomposition

Multi-step task decomposition into executable actions

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.task_decomposition import TaskDecomposition

module = TaskDecomposition()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  task_decomposition: true  # or false to disable
```

## Testing

```bash
pytest tests/task_decomposition/
```
