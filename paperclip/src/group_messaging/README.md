# group_messaging

Group messaging across platforms

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.group_messaging import GroupMessaging

module = GroupMessaging()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  group_messaging: true  # or false to disable
```

## Testing

```bash
pytest tests/group_messaging/
```
