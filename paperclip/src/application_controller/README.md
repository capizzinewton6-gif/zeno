# application_controller

Close, switch, and control applications

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.application_controller import ApplicationController

module = ApplicationController()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  application_controller: true  # or false to disable
```

## Testing

```bash
pytest tests/application_controller/
```
