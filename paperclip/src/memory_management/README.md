# memory_management

Long-context memory and conversation history

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.memory_management import MemoryManagement

module = MemoryManagement()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  memory_management: true  # or false to disable
```

## Testing

```bash
pytest tests/memory_management/
```
