# web_search

Intelligent web searching and results parsing

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.web_search import WebSearch

module = WebSearch()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  web_search: true  # or false to disable
```

## Testing

```bash
pytest tests/web_search/
```
