# folder_search

Folder and directory searching

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.folder_search import FolderSearch

module = FolderSearch()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  folder_search: true  # or false to disable
```

## Testing

```bash
pytest tests/folder_search/
```
