# unit_conversion

Unit conversion utilities

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.unit_conversion import UnitConversion

module = UnitConversion()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  unit_conversion: true  # or false to disable
```

## Testing

```bash
pytest tests/unit_conversion/
```
