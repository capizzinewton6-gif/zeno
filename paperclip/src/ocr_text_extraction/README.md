# ocr_text_extraction

OCR text extraction from images

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.ocr_text_extraction import OcrTextExtraction

module = OcrTextExtraction()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  ocr_text_extraction: true  # or false to disable
```

## Testing

```bash
pytest tests/ocr_text_extraction/
```
