# location_detection

Location detection services

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.location_detection import LocationDetection

module = LocationDetection()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  location_detection: true  # or false to disable
```

## Testing

```bash
pytest tests/location_detection/
```
