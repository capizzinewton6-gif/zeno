# terminal_execution

Terminal command execution

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.terminal_execution import TerminalExecution

module = TerminalExecution()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  terminal_execution: true  # or false to disable
```

## Testing

```bash
pytest tests/terminal_execution/
```
