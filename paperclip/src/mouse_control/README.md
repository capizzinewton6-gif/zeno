# mouse_control

Human-like mouse automation and control

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.mouse_control import MouseControl

module = MouseControl()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  mouse_control: true  # or false to disable
```

## Testing

```bash
pytest tests/mouse_control/
```
