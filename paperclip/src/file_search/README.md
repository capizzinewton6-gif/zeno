# file_search

Intelligent file searching by name and content

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.file_search import FileSearch

module = FileSearch()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  file_search: true  # or false to disable
```

## Testing

```bash
pytest tests/file_search/
```
