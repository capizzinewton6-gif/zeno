# calculator_engine

Calculator and mathematical operations

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.calculator_engine import CalculatorEngine

module = CalculatorEngine()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  calculator_engine: true  # or false to disable
```

## Testing

```bash
pytest tests/calculator_engine/
```
