# screenshot_processing

Screenshot capture and processing

## Overview

This is an independent capability module in the Paperclip system.

## Features

- Feature 1
- Feature 2
- Feature 3

## Usage

```python
from src.screenshot_processing import ScreenshotProcessing

module = ScreenshotProcessing()
result = module.execute("task description")
```

## Configuration

Edit `config.yaml` to enable/disable this module:

```yaml
capabilities:
  screenshot_processing: true  # or false to disable
```

## Testing

```bash
pytest tests/screenshot_processing/
```
